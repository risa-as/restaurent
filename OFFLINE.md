# نظام العمل بدون إنترنت (Offline) — التوثيق

هذا الملف يوثّق معمارية الأوف لاين في النظام وكل التغييرات المتعلقة بها.

> **آخر تحديث:** يغطّي تحويل الصفحات الخدمية إلى CSR، التخزين المؤقت المعزول، طابور المزامنة، التدفّق المحلي على جهاز واحد، ومزامنة الحالة + الفاتورة + الوردية + المخزون.

---

## 1. نظرة عامة على المعمارية

النظام **ويب (Next.js) متصل بقاعدة Neon السحابية**. لجعل الصفحات الخدمية تعمل بدون إنترنت تم اعتماد:

| الطبقة | الآلية |
|--------|--------|
| **القراءة** | كل صفحة خدمية تحوّلت إلى CSR وتجلب بياناتها عبر `useOfflineData` مع رجوع إلى IndexedDB |
| **الكتابة** | الطلبات تُحفظ محلياً (طابور + طلب مرئي) وتُزامَن عند عودة النت |
| **التدفّق المحلي** | على جهاز/متصفح واحد، الطلب ينتقل كاشير → مطبخ → نادل → كابتن عبر IndexedDB مشترك |

**الصفحات الخدمية الخمس (CSR + offline):** `/kitchen`، `/captain`، `/cashier`، `/waiter`، `/delivery` (+ `/captain/orders`).

---

## 2. القراءة أوف لاين — `useOfflineData`

`src/hooks/use-offline-data.ts`

```
useOfflineData<T>(apiUrl, cacheKey)
```

- يجلب من الـ API → عند النجاح يخزّن النتيجة في IndexedDB (`page_cache`).
- عند الفشل/المهلة → يرجع إلى آخر نسخة مخزّنة.
- **مهلة ثابتة 15 ثانية** (لا يعتمد على `navigator.onLine` لأنه غير موثوق).
- **إعادة محاولة تلقائية كل 8 ثوانٍ** أثناء وضع عدم الاتصال → الصفحة تتعافى ذاتياً.
- `refetch` صامت (لا يُظهر spinner الصفحة كاملة) — للتحديثات الخلفية.

### التخزين المعزول بـ tenantId 🔒
مفتاح التخزين يتضمّن `tenantId` (مثل `cashier:${tenantId}`) — **يمنع اختلاط بيانات المطاعم** في متصفح واحد عند تبديل الحسابات.

المسارات: `/api/{kitchen,captain,cashier,waiter,delivery}/data` و`/api/captain/orders/data`.

---

## 3. الكتابة أوف لاين — الطابور والطلبات المحلية

### أ. طابور المزامنة (`sync_queue`)
`src/lib/offline/queue.ts` + `src/lib/offline/db.ts`

- أي إجراء يفشل أوف لاين يُحفظ كـ `QueuedAction` (`CREATE_ORDER`، `UPDATE_ORDER_STATUS`، `UPDATE_ITEM_STATUS`، `CLOSE_BILL`).
- `syncQueue()` يُرسل كل إجراء إلى `/api/offline/sync` ويُطلق تلقائياً عند حدث `online`.
- عند نجاح المزامنة بالكامل → يُمسح مخزن الطلبات المحلية (`live_orders`) تجنّباً للتكرار.

### ب. الطلبات المحلية (`live_orders`) — التدفّق على جهاز واحد
مخزن IndexedDB (DB_VERSION 4) يحمل **الطلب الكامل** (أصناف، حالة، طاولة...) بحيث يظهر عبر كل الصفحات على نفس الجهاز.

دوال `db.ts`: `saveLiveOrder`، `getLiveOrders`، `getLiveOrder`، `updateLiveOrderItems`، `setLiveOrderStatus`، `clearLiveOrders`.

---

## 4. التدفّق الكامل بدون نت (جهاز واحد)

```
الكاشير ينشئ طلباً (أوف لاين)
  → يُحفظ كـ live_order كامل + يُضاف لطابور المزامنة (مع localOrderId)
المطبخ يقرأ live_orders ويعرضها  → "بدء التحضير"/"جاهز" تُحدّث live_order
النادل يرى الطلب الجاهز  → "تم التسليم" تُحدّث live_order (SERVED)
الكابتن (حالة الطلبات) يرى الطلب  → "تم التسليم" (COMPLETED)
  ↓ عند عودة النت
syncQueue يقرأ الحالة النهائية للطلب المحلي ويرسلها →
/api/offline/sync يُنشئ: الطلب (بالحالة النهائية) + الفاتورة + مجاميع الوردية + خصم المخزون
```

---

## 5. منطق المزامنة الكامل — `/api/offline/sync`

`src/app/api/offline/sync/route.ts` — `handleCreateOrder`:

1. **أمان:** يتحقق أن مستأجر الجلسة = مستأجر الطلب + الدور مسموح.
2. **التسعير من القاعدة** (لا يثق بسعر العميل) — يشمل العروض + الإضافات + الضريبة + رسوم الخدمة.
3. **الحالة النهائية:** يُنشئ الطلب وأصنافه بالحالة المنقولة من الطلب المحلي (لا يعود PENDING).
4. **الفاتورة + الوردية:** للكاشير/المدير (غير التوصيل) — ينشئ `Bill` ويُحدّث `totalSales/totalCash/totalCard` للوردية المفتوحة (بحثاً بـ `cashierId`).
5. **المخزون:** إذا كانت الحالة `SERVED`/`COMPLETED` → `deductOrderStock` في **معاملة منفصلة** (كي لا يُفقد الطلب لو كان المخزون غير كافٍ).
6. **الإشعارات:** يُطلق أحداث Pusher (kitchen/orders) فيدخل الطلب الدورة الطبيعية.

---

## 6. الإصلاحات الأمنية والدقّة المرتبطة

| الإصلاح | الملف | الوصف |
|---------|-------|-------|
| تسريب الورديات بين الكاشيرين | `lib/actions/shifts.ts` | `getCurrentShift` كان يستخدم `user.id` (= undefined) فيرجع أي وردية بالمطعم. صُحّح إلى `user.userId` |
| هوية سجلّ التدقيق | `lib/audit.ts` | نفس الخطأ `user.id` → `user.userId` |
| احتساب مبيعات الوردية | `lib/actions/pos.ts` | عند انتهاء كوكي الوردية، يبحث عن الوردية بـ `cashierId` فلا تفوت المبيعات |
| كوكي الوردية | `lib/actions/shifts.ts` | مُدّد من 8 إلى 18 ساعة |
| مودال الوردية الكاذب | `cashier-page-client.tsx` | لا يُجبر المودال أثناء عرض بيانات مخزّنة (`!isOffline`) |
| رقم أمر الشراء | `lib/actions/purchase-orders.ts` + schema | فريد لكل مستأجر `@@unique([tenantId, poNumber])` + توليد آمن من التصادم |
| المصادقة في الإنتاج | `lib/auth.ts` + `auth.config.ts` | `trustHost: true` (يحل `UntrustedHost`) |
| تحديثات الواجهة الفورية | الصفحات الخمس | `onRefresh = refetch` بعد كل إجراء + في Pusher/polling |

---

## 7. كيفية التجربة

```bash
npm run build   # مطلوب — تعديلات خادم
npm start
```

1. سجّل دخول، افتح الكاشير/المطبخ/النادل/الكابتن في تبويبات على **نفس المتصفح**.
2. أطفئ النت.
3. الكاشير: أنشئ طلباً → "تم حفظ الطلب محلياً".
4. المطبخ: الطلب يظهر → "بدء التحضير" → "جاهز".
5. النادل: الطلب الجاهز يظهر → "تم التسليم".
6. الكابتن (حالة الطلبات): يظهر → "تم التسليم".
7. أعد النت → يُرفع للسحابة (حالة نهائية + فاتورة + وردية + مخزون).

---

## 8. الحدود المعروفة

| الحد | التفصيل |
|------|---------|
| **جهاز واحد** | التدفّق المحلي يعمل على متصفح واحد فقط. الأجهزة المنفصلة على شبكة المطعم (LAN) تحتاج **خادماً محلياً + قاعدة بيانات محلية** (تحويل معماري أكبر) |
| **بطء أوّل تحميل في dev** | في بيئة التطوير، أول زيارة لكل مسار تُجمَّع لحظياً (بطيء). لا يحدث في الإنتاج |
| **المخزون عند نقص الكمية** | لو نفد المخزون وقت المزامنة، يُحفظ الطلب (مالياً) لكن لا يُخصم المخزون — يُسجَّل تحذير في السجلّ |

---

## 9. الملفات الرئيسية

```
src/hooks/use-offline-data.ts          ← جلب + cache + إعادة محاولة
src/lib/offline/db.ts                  ← IndexedDB (page_cache, sync_queue, live_orders)
src/lib/offline/queue.ts               ← المزامنة + نقل الحالة النهائية
src/lib/offline-queue.ts               ← shim لـ enqueueOrder/drainQueue
src/app/api/offline/sync/route.ts      ← منطق المزامنة الكامل (طلب+فاتورة+وردية+مخزون)
src/app/api/*/data/route.ts            ← مصادر بيانات الصفحات الخدمية
src/components/{cashier,kitchen,waiter,captain,delivery}/*  ← مكوّنات CSR + الدمج المحلي
```
