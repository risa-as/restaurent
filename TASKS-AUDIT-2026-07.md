# قائمة مهام الفحص الشامل — 2026-07-02

نتيجة فحص شامل: تقني + أمني + أداء + نظام الأوفلاين (الكابتن، الكاشير، الويتر، المطبخ).
الحالة العامة: TypeScript نظيف (0 أخطاء)، ESLint تحذيرات فقط، العزل بين المستأجرين محكم (fail-closed)، ومعمارية الأوفلاين سليمة من حيث التصميم — لكن توجد **ثغرتان حرجتان في الأوفلاين تسببان فقدان/تكرار طلبات**.

> الأولويات: 🔴 P0 حرج (فقدان بيانات) · 🟠 P1 مهم قبل الإطلاق · 🟡 P2 متوسط · 🔵 P3 تحسين

---

## أ) نظام الأوفلاين

### 🔴 T1 — طلبات الكابتن الأوفلاين تُفقد نهائياً (P0)
- **الملفات:** `src/components/captain/captain-order-form.tsx:188-217`، `src/lib/offline-queue.ts:20`، `src/app/api/offline/sync/route.ts:39`
- **المشكلة:** `orderPayload` في الكابتن لا يتضمن `tenantId` → يُحفظ في الطابور بـ `tenantId: ''` → نقطة المزامنة ترفضه 400 → `queue.ts` يعامل 4xx كخطأ غير قابل لإعادة المحاولة → **يُحذف الطلب من الطابور بصمت** رغم أن التوست وعد المستخدم "سيرسل تلقائياً عند عودة الإنترنت".
- **الحل:** إضافة `tenantId` (متوفر كـ prop) إلى الحمولة — كما يفعل الكاشير.

### 🔴 T2 — تكرار الطلبات عند المزامنة من عدة تبويبات (P0)
- **الملفات:** `src/lib/offline/queue.ts:32-95`
- **المشكلة:** التشغيل الموثق يفتح الكاشير+المطبخ+الويتر+الكابتن في تبويبات على نفس المتصفح. كل تبويب يسجّل مستمع `online` ويشغّل `syncQueue()`. علم `syncing` محلي لكل تبويب، بينما الطابور مشترك في IndexedDB → تبويبان يقرآن نفس الطلب ويرسلانه معاً قبل حذفه → **طلب/فاتورة/مجاميع وردية مكررة**. كذلك إعادة المحاولة بعد نجاح فعلي ضاع ردّه على الشبكة تكرر الطلب.
- **الحل (ممارسة أفضل قياسية):**
  1. **قفل عبر التبويبات:** لفّ `syncQueue` بـ `navigator.locks.request('offline-sync', ...)` (Web Locks API) حتى لا يزامن إلا تبويب واحد.
  2. **مفتاح Idempotency على الخادم:** إرسال `action.id` (موجود أصلاً في الجسم) وتخزينه في عمود `clientRequestId` على Order مع قيد `@@unique([tenantId, clientRequestId])` — إن وُجد مسبقاً يُرجع الخادم الطلب الموجود بدل إنشاء جديد. هذا يحمي من كل أشكال التكرار.

### 🟠 T3 — الطابور لا يُصرَّف عند فتح التطبيق وهو متصل (P1)
- **الملفات:** `src/lib/offline/queue.ts:102-106`، `src/components/offline/network-status-banner.tsx:40`
- **المشكلة:** المزامنة تنطلق فقط من حدث `online`. إن أُغلق المتصفح أثناء الانقطاع وفُتح لاحقاً وهو متصل، لا يحدث الحدث → الإجراءات المعلقة تبقى للأبد، والبانر يعيد `null` في حالة (متصل + معلق + idle) فلا يرى المستخدم شيئاً.
- **الحل:** عند التحميل (mount) إذا `pendingCount > 0 && navigator.onLine` → تشغيل `syncQueue()`؛ وإظهار حالة "معلق" في البانر بدل إخفائها.

### 🟠 T4 — حذف صامت للإجراءات الفاشلة (P1)
- **الملفات:** `src/lib/offline/queue.ts:46-49, 76-78`
- **المشكلة:** بعد 3 محاولات فاشلة (خطأ خادم مستمر) أو أي رد 4xx، يُحذف الإجراء نهائياً بلا أي إشعار — قد يكون طلباً بمبلغ مالي.
- **الحل:** نقل الإجراء إلى مخزن `failed_actions` بدل الحذف + شارة في الواجهة تعرض التفاصيل مع زر إعادة محاولة/تصدير. لا يُحذف إجراء `CREATE_ORDER` أبداً دون علم المستخدم.

### 🟠 T5 — "تم التسليم" للويتر أوفلاين على طلب خادم: يفشل ويُعلّق الزر (P1)
- **الملفات:** `src/components/waiter/waiter-service-view.tsx:300-323`، `src/app/api/offline/sync/route.ts:13`
- **المشكلة:** `serveOrder()` عند انقطاع الشبكة يرمي استثناءً غير مُلتقط → لا يُقيَّد في الطابور، و`setLoadingId(null)` لا يُنفَّذ (زر معلّق). نفس النمط في `client-order-card.tsx` للكابتن. إضافة إلى أن `STATUS_UPDATE_ROLES` في نقطة المزامنة لا تشمل WAITER/CAPTAIN أصلاً، فلو قُيِّد الإجراء لرُفض 403 وحُذف.
- **الحل:** try/catch يقيّد `UPDATE_ORDER_STATUS` في الطابور + توسيع أدوار المزامنة (WAITER: تحويل إلى SERVED فقط؛ CAPTAIN: حسب تدفقه) + `finally` لتحرير الزر.

### 🟠 T6 — إعدادات كاش PWA المخصصة لا تصل إلى الـ Service Worker (P1)
- **الملفات:** `next.config.mjs` (كتلة `runtimeCaching`)، والمولَّد `public/sw.js`
- **المشكلة (مؤكدة بالفحص):** حزمة `@ducanh2912/next-pwa` تتوقع `workboxOptions.runtimeCaching` وليس `runtimeCaching` في المستوى الأعلى — الفحص الفعلي لـ `public/sw.js` يظهر القواعد الافتراضية فقط (لا وجود لـ `operational-api`/`operational-pages`/`menu-api`). أي أن سياساتك المقصودة (عدم كاش صفحات الإدارة، كاش 5 دقائق لمسارات التشغيل، مهلات مخصصة) **غير مفعّلة**. الأنماط نفسها خاطئة أيضاً (`/^\/api\//` لا يطابق URL كاملاً يبدأ بـ https).
- **الحل:** نقل القواعد إلى `workboxOptions.runtimeCaching` مع أنماط دوال `({ url, sameOrigin }) => ...` بدل regex المسار، ثم rebuild والتحقق من `public/sw.js`.

### 🟡 T7 — طلب الكابتن الأوفلاين لا يظهر في المطبخ (P2)
- **المشكلة:** الكاشير فقط ينشئ `live_order` (التدفق المحلي على جهاز واحد). في وضع TABLE_SERVICE الكابتن هو مُدخل الطلبات الأساسي، وطلبه الأوفلاين لا يُرى في المطبخ حتى تعود الشبكة.
- **الحل:** تعميم نمط `buildLiveOrder` + `localOrderId` من `cashier-view.tsx` على نموذج الكابتن.

### 🟡 T8 — إسناد الفرع في المزامنة لا يستخدم `resolveCreateBranchId` (P2)
- **الملفات:** `src/app/api/offline/sync/route.ts:208`
- **المشكلة:** `branchId: userBranchId ?? null` — إن لم يكن للمستخدم فرع معيّن يُنشأ الطلب بفرع NULL، بينما الطلبات الأونلاين تذهب للفرع الرئيسي عبر `resolveCreateBranchId` (نموذج الفروع نصّ على إزالة بيانات NULL-branch). تقارير الفروع ستفقد هذه الطلبات.
- **الحل:** استخدام `resolveCreateBranchId(tenantId, userBranchId)`.

### 🟡 T9 — `CLOSE_BILL` كود ميت وناقص (P2)
- **الملفات:** `src/app/api/offline/sync/route.ts:365-385`
- **المشكلة:** لا يوجد أي عميل يقيّد `CLOSE_BILL` في الطابور، ومعالجه لا ينشئ `Bill` ولا يحدّث مجاميع الوردية — فقط يغيّر الحالة إلى COMPLETED. أي أن **تحصيل فاتورة طاولة قائمة (تدفق TABLE_SERVICE) لا يعمل أوفلاين إطلاقاً**.
- **الحل:** إما تنفيذ التدفق كاملاً (تقييد من واجهة الكاشير + إنشاء Bill + وردية + خصم مخزون في المعالج)، أو حذف الحالة وتوثيق أن تحصيل الفواتير يتطلب اتصالاً.

### 🟡 T10 — كاش الـ Service Worker لا يُمسح عند تسجيل الخروج (P2)
- **المشكلة:** القاعدة الافتراضية `apis` (NetworkFirst) تخزّن ردود JSON مصادَق عليها في Cache Storage. `clearOfflineCaches()` يمسح IndexedDB فقط. على جهاز مشترك/نشر أحادي النطاق قد تُقدَّم بيانات مستأجر سابق من الكاش أثناء الانقطاع.
- **الحل:** عند الخروج: `caches.keys().then(ks => ks.forEach(k => caches.delete(k)))` بجانب مسح IndexedDB.

### 🔵 T11 — تكرار العرض عند مزامنة جزئية (P3)
- `clearLiveOrders()` يعمل فقط عندما يصبح الطابور فارغاً بالكامل. إن علِق إجراء آخر، يظهر الطلب المحلي + نسخته الخادمية معاً في المطبخ/الويتر. الحل: حذف الـ live order المقابل فور نجاح `CREATE_ORDER` الخاص به (بمعرّف `localOrderId`).

### 🔵 T12 — تحسينات صغيرة (P3)
- مهلة الـ 15 ثانية في `use-offline-data.ts` لا تلغي الـ fetch الجاري (استخدم `AbortSignal.timeout` أو اربط المهلة بالـ controller).
- `openDB()` بلا معالج `onversionchange` — ترقية إصدار DB مستقبلاً ستُحجب إذا كان تبويب قديم مفتوحاً.

### ✅ ما هو مطبَّق بشكل صحيح (للاطمئنان)
- القراءة: `useOfflineData` + `page_cache` معزول بـ tenantId، التعامل المحافظ مع `navigator.onLine`، backoff أسي، حارس الـ abort للـ loading — سليم بعد إصلاحات 2026-07-01.
- الكتابة: طابور موحّد بلا ازدواجية (إصلاح المخزن القديم)، نقل الحالة النهائية للطلب المحلي، التسعير من القاعدة (لا يثق بالعميل)، فاتورة+وردية+مخزون في المزامنة، خصم المخزون في معاملة منفصلة.
- الواجهة: `NetworkStatusBanner` مركّب في المخططات الخمسة، `OfflineFallbackBanner` بعمر الكاش.
- **الخلاصة المعمارية:** CSR + IndexedDB + طابور مزامنة + تدفق محلي أحادي الجهاز هي الممارسة الصحيحة لـ POS ويب. الناقص عن المعيار الصناعي: idempotency (T2)، قفل التبويبات (T2)، عدم إسقاط الإجراءات (T4)، وتصريف الطابور عند الإقلاع (T3). العمل متعدد الأجهزة على LAN بدون إنترنت يبقى خارج النطاق (يتطلب خادماً محلياً) كما وثّقت OFFLINE.md.

---

## ب) الأمان

### 🟠 T13 — ملفا middleware مزدوجان: احذف النسخة الميتة (P1)
- **الملفات:** `middleware.ts` (الجذر) و `src/middleware.ts`
- لأن التطبيق داخل `src/`، يستخدم Next.js ملف `src/middleware.ts` (المتكامل: rate limiting + CORS + فرض 2FA + حلّ slug المستأجر) ويتجاهل ملف الجذر (نسخة مبسطة بدون أي من ذلك). وجود الملفين خطر التباس: من يعدّل ملف الجذر يظن أنه غيّر سلوك الحماية. **تحقَّق** بأن مخرجات `next build` تُظهر `ƒ Middleware` بحجم يعكس النسخة الكاملة، ثم احذف `middleware.ts` من الجذر.

### 🟡 T14 — مسارات `/api/*/data` بلا فحص أدوار (P2)
- المسارات الخمسة تفحص الجلسة فقط. ويتر أو شيف يستطيع طلب `/api/cashier/data` وقراءة مجاميع الوردية والمبيعات. أضِف allowlist أدوار لكل مسار (مبدأ أقل صلاحية).

### 🟡 T15 — تقوية الـ rate limiting (P2)
- في الذاكرة لكل نسخة (غير فعّال متعدد النسخ/serverless) — النقل إلى Redis/Vercel KV ما زال معلقاً من الفحص السابق.
- `isLocalRequest`: عندما `ip === 'unknown'` يُتخطى الحد كلياً — اتصال مباشر بالخادم بلا `x-forwarded-for` يتجاوز كل الحدود. اجعل التخطي في التطوير فقط.

### 🔵 T16 — تشديد CSP (P3)
- `script-src` يتضمن `'unsafe-eval'` و`'unsafe-inline'`. جرّب إزالة `'unsafe-eval'` في الإنتاج (غالباً غير مطلوب مع Next بعد البناء)، ولاحقاً نمط nonce.

### ✅ سليم
- عزل fail-closed في `getTenantScope` + حقن tenantId لـ 28 نموذجاً، `.env` خارج git، رؤوس أمنية (CSP/nosniff/DENY)، فرض 2FA للسوبر أدمن، أسعار من القاعدة في كل مسارات الإنشاء، حماية IDOR المصلَحة سابقاً ما زالت قائمة.

---

## ج) الأداء

### 🟠 T17 — إعدادات اتصال Neon (P1 — إجراء بنية تحتية)
- أضِف `pgbouncer=true` إلى `DATABASE_URL` (pooler موجود لكن بدون العلم — Prisma قد يستخدم prepared statements غير مدعومة عبر PgBouncer transaction mode).
- `connect_timeout=15&pool_timeout=15` تساوي مهلة العميل 15 ثانية — انتظار الـ pool وحده يستهلك كامل مهلة الواجهة. اخفضها إلى ~10.
- عالج cold start الخاص بـ autosuspend (تعطيله أو keep-warm) — هو سبب "البطء المتقطع".

### 🟡 T18 — دمج استعلامات السياق المتكررة لكل طلب (P2)
- كل طلب يشغّل `getTenantScope` (استعلام Tenant) + `getActiveBranchId` (استعلام Tenant ثانٍ + جلسة). ادمجهما في استعلام واحد memoized يعيد `{ id, slug, isActive, multiBranchEnabled }`.

### 🟡 T19 — تقليم حمولات مسارات البيانات (P2)
- `getKitchenOrders` وأشباهها تستخدم `include` عميقاً بلا `select` — كل حقول MenuItem والفئة تُرسل في JSON كل 10 ثوانٍ. حدّد الحقول المطلوبة فقط (id، name، nameAr، price، categoryId...).

### 🔵 T20 — تحسينات أخرى (P3)
- `captain-orders-client` يعمل polling كل 10 ثوانٍ — وحّده مع نمط Pusher المستخدم في بقية الصفحات.
- قياس الأداء على بناء إنتاجي (`next build && next start`) لا على `next dev`.
- فحص الحزم بـ `@next/bundle-analyzer` والتأكد أن recharts/leaflet تُحمَّل ديناميكياً في صفحات الداشبورد فقط.

---

## د) جودة تقنية

### 🔵 T21 — تحذيرات ESLint (P3)
- `waiter-service-view.tsx:263` — `react-hooks/exhaustive-deps`: مُعتمدية `tenantId` ناقصة (حقيقية، أصلحها).
- عدة مكونات dashboard/superadmin تستخدم `<img>` بدل `next/image`.

### 🔵 T22 — تغييرات غير مرفوعة (P3)
- 3 ملفات معدّلة في شجرة العمل (حارس loading في `use-offline-data`، فلتر الفرع في `getCaptainMenu`، إصلاح تمدد تبويبات الكابتن) — إصلاحات جيدة، ارفعها بـ commit.

---

## ترتيب التنفيذ المقترح
1. **اليوم:** T1 (سطر واحد يمنع فقدان طلبات) + T22 (commit)
2. **هذا الأسبوع:** T2 (idempotency + web lock) → T3 → T4 → T5 → T13
3. **قبل الإطلاق:** T6، T8، T9، T14، T17
4. **بعد الإطلاق:** البقية (P2/P3)
